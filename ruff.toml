target-version = "py310"
line-length = 100

[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line length -- formatter handles this
    "E722",  # bare except -- intentional in recon error handling
]

[lint.isort]
known-first-party = ["domino"]
